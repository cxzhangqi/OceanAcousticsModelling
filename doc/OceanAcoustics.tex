\documentclass{report}
\title{Ocean Acoustics Modelling}
\author{Aaron Kaw}
\date{}

\input{C:/Users/aaron/OneDrive/Science/Programming/LaTeX/AaronLibrary.tex}

\newcommand{\bbR}{\mathbb{R}}
\newcommand{\bbC}{\mathbb{C}}

\newcommand{\calR}{\mathcal{R}}
\newcommand{\calZ}{\mathcal{Z}}

\newcommand{\BL}{\text{BL}}

\begin{document}
\maketitle
\tableofcontents

\chapter{Ocean Parameters}
\section{Attenuation}
\subsection{Units}
TODO: Re-express all ocean parameters (except frequency) as complex. Julia implementation shows such.

We see from \cite{jensen2011computational} that the sound speed is often treated as a complex-valued parameter. However, Jensen is quite unclear as to how to define said parameter, regarding which other acoustic parameters are complex-valued and which are not.

From Jensen we take the normalized sound speed solution for plane wave attenuation
\M{
	\exp\Par{\frac{i\omega x}{c}}
}
with $c \in \bbC$, and from \cite{buckingham1997theory} we take the same normalized solution (with variables translated) to obtain
\M{
	\exp\Par{-\frac{i\omega x + \beta\omega x}{c^{(p)}}}
}
and equate the (principle) arguments so
\A{
	&& \frac{i\omega x}{c} &= -\frac{i\omega x + \delta\omega x}{c^{(p)}} \\
	\Rightarrow && \frac{c}{i} &= -\frac{c^{(p)}}{i + \delta} \\
	\Rightarrow && c &= -\frac{ic_p}{i + \delta} \\
	\Rightarrow && c &= -\frac{-c^{(p)}}{-1 + i\delta} \\
	\Rightarrow && c &= \frac{c^{(p)}}{1 - i\delta} \\
	\Rightarrow && c &= c^{(p)}\frac{1 + i\delta}{1 + \delta^2}
}
It is of interest to note that the sound speed retains its magnitude, $\Abs{c} = c^{(p)}$

From \cite{jensen2011computational} we have the relationship of the loss tangent as
\A{
	&& ikx\Par{1 + i\delta} &= ikx - \alpha x \\
	\Rightarrow && 1 + i\delta &= 1 - \frac{\alpha}{ik} \\
	\Rightarrow && i\delta &= \frac{i\alpha}{k} \\
	\Rightarrow && \delta &= \frac{\alpha}{k}
}
where since the loss tangent $\delta$ and attenuation $\alpha$ are real, $k$ must also be real here. Note that here, the units of $\alpha$ are nepers/m.

For $\alpha^{(\lambda)}$ which is in dB/$\lambda$,
\M{
	\alpha^{(\lambda)} = \alpha\lambda 20\log_{10}(e)
}
so
\A{
	\delta &= \frac{\alpha}{k} \\
	&= \frac{\alpha^{(\lambda)}}{k\lambda 20\log_{10}(e)} \\
	&= \frac{\alpha^{(\lambda)}}{40\pi\log_{10}(e)}
}

\section{Bottom Loss}
\M{
	\calZ_1 = \frac{\rho_1 \varsigma_1}{\sin(\theta_1)} \\
	\calZ_p = \frac{\rho_2 \varsigma_p}{\sin(\theta_p)} \\
	\calZ_s = \frac{\rho_2 \varsigma_s}{\sin(\theta_s)} \\
	k_p\cos(\theta_p) = k_s\cos(\theta_s) = k_1\cos(\theta_1) \\
	\calZ_\text{tot} = \calZ_p\cos^2(2\theta_s) + \calZ_s\sin^2(2\theta_s) \\
	\calR = \frac{\calZ_\text{tot} - \calZ_1}{\calZ_\text{tot} + \calZ_1} \\
	\BL = -10\log_{10}\Abs{\calR}^2
}

\chapter{Ray Tracing}
\M{
	\Dif{r}{z} = \cot\Par{\theta(z)}
}
\section{Eikonal Equation}


\bibliographystyle{apalike}
\bibliography{OAM}

\end{document}